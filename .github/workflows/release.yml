name: Release

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  release-policy:
    runs-on: ubuntu-22.04
    steps:
      - name: Confirm release policy
        run: |
          echo "Automated package publishing is disabled for this fork."
          echo "A later OpenSpec release phase must define package names, registry targets, and credentials first."
