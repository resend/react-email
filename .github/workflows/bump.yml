name: Version Packages

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  version-policy:
    runs-on: ubuntu-22.04
    steps:
      - name: Confirm version policy
        run: |
          echo "Automated version-package pull requests are disabled for this fork."
          echo "A later OpenSpec release phase must define package names, registry targets, and credentials first."
