import { Layout } from "../_components/layout";
import { component } from "./Container with section/inline-styles";

export default () => {
  return (
    <Layout>
      {component}
    </Layout>
  );
};
