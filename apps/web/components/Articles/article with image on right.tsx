import { Layout } from "../_components/layout";
import { component } from "./Article with image on right/inline-styles";

export const title = "Article with image on the right";

export default () => {
  return (
    <Layout>
      {component}
    </Layout>
  );
};
