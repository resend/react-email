import { Layout } from "../_components/layout";
import { component } from "./Header with four paragraphs and two columns/inline-styles";

export default () => {
  return <Layout>{component}</Layout>;
};
