import { Layout } from "../_components/layout";
import { component } from "./One row, three columns/inline-styles";

export default () => {
  return (
    <Layout>
      {component}
    </Layout>
  );
};
