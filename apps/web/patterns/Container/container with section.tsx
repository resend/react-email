import { Layout } from "../_components/layout";
import { pattern } from "./Container with section/inline-styles";

export default () => {
  return (
    <Layout>
      {pattern}
    </Layout>
  );
};
