import { Layout } from "../_components/layout";
import { pattern } from "./One row, three columns/inline-styles";

export default () => {
  return (
    <Layout>
      {pattern}
    </Layout>
  );
};
