import { Layout } from "../_components/layout";
import { pattern } from "./Article with image as background/inline-styles";

export default () => {
  return (
    <Layout>
      {pattern}
    </Layout>
  );
};
