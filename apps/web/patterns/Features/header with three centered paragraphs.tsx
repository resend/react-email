import { Layout } from "../_components/layout";
import { pattern } from "./Header with three centered paragraphs/inline-styles";

export default () => {
  return (
    <Layout>
    {pattern}
    </Layout>
  );
};
