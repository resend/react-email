import { Layout } from "../_components/layout";
import { pattern } from "./Code inline with different colors/inline-styles";

export default () => {
  return (
    <Layout>
      {pattern}
    </Layout>
  );
};
