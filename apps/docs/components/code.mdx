---
title: "@react-email/code"
sidebarTitle: "Code"
description: "A block of code to be rendered in your email."
---

import Support from "../snippets/support.mdx";
import License from "../snippets/license.mdx";

<img className="rounded-md" src="/images/components/code.png" alt="code" />

## Install

Install component from your command line.

<CodeGroup>

```sh yarn
yarn add @react-email/code -E
```

```sh npm
npm install @react-email/code -E
```

</CodeGroup>

## Getting started

Add the component to your email template. Include styles where needed.

```jsx
import { Container } from "@react-email/container";
import { Code } from "@react-email/code";

const Email = () => {
  return (
    <Container>
      <Code style={{ color: "#000" }}>const foo = 'bar';</Code>
    </Container>
  );
};
```

<License />
