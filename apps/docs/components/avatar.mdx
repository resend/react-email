---
title: 'Avatar'
sidebarTitle: 'Avatar'
description: 'Displays a profile picture or a fallback to the user initials.'
'og:image': 'https://react.email/docs/images/components/avatar.png'
---

import Support from '../snippets/support.mdx';

## Install

Install component from your command line.

<CodeGroup>

```sh npm
npm install @react-email/avatar -E
```

```sh yarn
yarn add @react-email/avatar -E
```

</CodeGroup>

## Getting started

Add the component to your email template. Include styles where needed.

Avatar

```jsx
import { Avatar, AvatarImage, AvatarFallback } from '@react-email/avatar';

const Email = () => {
  return (
    <Avatar>
      <AvatarImage src="cat.jpg" />
      <AvatarFallback>My Cat</AvatarFallback>
    </Avatar>
  );
};
```

## Try it live

<Card
  title="Avatar preview"
  icon={duotone('arrow-up-right-from-square')}
  href="https://demo.react.email/packages/avatar-image"
>
  See how this component looks in a real email.
</Card>

## Props

<ResponseField name="src" type="string">
  The path to the image
</ResponseField>

<Support />
