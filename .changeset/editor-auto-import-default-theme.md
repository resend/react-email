---
"@react-email/editor": patch
---

Ship the default theme CSS import in `EmailEditor`'s published output, so bubble menus and the slash command menu render styled without a manual `@react-email/editor/themes/default.css` import.
