---
'react-email': minor
'@react-email/ui': minor
---

You can now pass custom esbuild plugins with `--esbuild-plugins` to `email dev`, `email build` and `email export`, so templates that need a custom compile-time transform or plugin-resolved imports can be previewed and exported.
