---
"@react-email/editor": patch
---

Drive editor node defaults (headings, paragraphs, lists, blockquotes, buttons, code, hr) from the email theming reset, add `node-list` / `node-listItem` class hooks, and source `disc` / `decimal` list markers from new `bulletList` and `orderedList` reset entries.
