---
"@react-email/editor": minor
---

Allow theme customization via `ThemeConfig` API. The `theme` prop on `EmailEditor` now accepts a config object with CSS-in-JS styles keyed by component name, with optional inheritance from built-in themes. Adds `createTheme` and `extendTheme` helpers.
