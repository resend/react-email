---
"react-email": patch
"create-email": patch
---

Fix `TS2307: Cannot find module 'react-email'` in projects generated by `npx create-email@latest`. The generated `tsconfig.json` now sets `"module": "ESNext"` and `"moduleResolution": "bundler"` so TypeScript reads `react-email`'s `exports` field. `react-email/package.json` also gains a root-level `"types"` field as a backward-compatibility fallback for consumers using legacy `moduleResolution: node`/`node10`.
